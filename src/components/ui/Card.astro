---
type Props = {
  as?: 'article' | 'section' | 'div' | 'li';
  href?: string;
  /** `lift` is the layered card shadow plus a hover raise — for a card that
      sits on a coloured ground and has to read as an object on top of it. */
  elevation?: 'flat' | 'sm' | 'md' | 'lift';
  class?: string;
};

const { as: Tag = 'article', href, elevation = 'sm', class: className = '' } = Astro.props as Props;
const classes = ['card', `card--${elevation}`, href && 'card--link', className]
  .filter(Boolean)
  .join(' ');
---

{
  href ? (
    <a class={classes} href={href}>
      <slot />
    </a>
  ) : (
    <Tag class={classes}>
      <slot />
    </Tag>
  )
}

<style>
  .card {
    display: block;
    /* Consumers repaint the ground by setting --card-bg on any ancestor —
       the component stays dumb and nobody has to reach in at .card. */
    background: var(--card-bg, var(--bg-elevated));
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    border: 1px solid var(--border);
    color: inherit;
    text-decoration: none;
  }

  .card--flat {
    box-shadow: none;
  }
  .card--sm {
    box-shadow: var(--shadow-sm);
  }
  .card--md {
    box-shadow: var(--shadow-md);
  }
  .card--lift {
    box-shadow: var(--shadow-card);
    transition:
      transform var(--dur-base) var(--ease-out),
      box-shadow var(--dur-base) var(--ease-out);
  }
  .card--lift:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-card-hover);
  }

  .card--link {
    transition:
      transform var(--dur-fast) var(--ease-out),
      box-shadow var(--dur-fast) var(--ease-out);
  }

  .card--link:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
</style>
